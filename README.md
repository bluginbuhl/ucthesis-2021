# LaTex Template for UC Santa Barbara Dissertation

Developed and tested on Ubuntu 20.04. Requires `pdflatex` to be installed.

Usage:
```bash
$ make all
```

will run the `all` command in `Makefile`.